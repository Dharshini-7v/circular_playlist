services:
  - type: web
    name: circular-playlist
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn playlist_api.server:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
